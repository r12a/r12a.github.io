var defaults = new Object;

defaults.font = 'Myanmar3 WF' // font name per usage in CSS
defaults.size = "32";  // number of pixels (just the number)
defaults.rows = "1.5"; // number representing number of times 100px for height of box
defaults.view = "alphabet";  // alphabet, fontgrid
defaults.language = 'my'
defaults.blocklocation= '/scripts/myanmar/block'
defaults.uifont = 'Myanmar3 WF'; // font name per usage in CSS
defaults.uisize = "28";  // number of pixels (just the number)
defaults.ccbase = "";  // default base for combining characters


