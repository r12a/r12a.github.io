var defaults = new Object;

defaults.font = "Khmer OS Battambang WF"; // font name per usage in CSS
defaults.size = "40";  // number of pixels (just the number)
defaults.rows = "2.4"; // number representing number of times 100px for height of box
defaults.lineHeight = "2";  // just the number
defaults.view = "alphabet";  // alphabet, phonic, phonic2, phonelist
defaults.language = 'km'
defaults.blocklocation= '/rishida/scripts/block/khmer'
defaults.uifont = "Khmer OS Battambang WF"; // font name per usage in CSS
defaults.uisize = "25";  // number of pixels (just the number)


