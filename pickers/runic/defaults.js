var defaults = new Object;

defaults.font = '"Noto Sans Runic WF"'; // font name per usage in CSS
defaults.size = "30";  // number of pixels (just the number)
defaults.rows = "3"; // number representing number of times 100px for height of box
defaults.lineHeight = "1.5";  // just the number
defaults.view = "alphabet";  // alphabet, phonic, phonelist
defaults.language = 'non'
defaults.blocklocation= '/scripts/runic/block'
defaults.uifont = '"Noto Sans Runic WF"'; // font name per usage in CSS
defaults.uisize = "40";  // number of pixels (just the number)
defaults.ccbase = "ـ";  // default base for combining characters


