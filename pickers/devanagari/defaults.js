var defaults = new Object;

defaults.font = 'Noto Sans Devanagari WF'; // font name per usage in CSS
defaults.size = "50";  // number of pixels (just the number)
defaults.rows = "2"; // number representing number of times 100px for height of box
defaults.lineHeight = "1.8";  // just the number
defaults.view = "alphabet";  // alphabet, phonic, phonelist
defaults.language = 'hi'
defaults.blocklocation= '/scripts/block/devanagari'
defaults.uifont = 'Noto Sans Devanagari WF'; // font name per usage in CSS
defaults.uisize = "28";  // number of pixels (just the number)


