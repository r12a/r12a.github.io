var defaults = new Object;

defaults.font = '"SolaimanLipi", "Lohit Bengali", "Bangla MN", "Bangla Sangam MN", "Noto Sans Bengali", sans-serif'; // font name per usage in CSS
defaults.size = "45";  // number of pixels (just the number)
defaults.rows = "1.5"; // number representing number of times 100px for height of box
defaults.lineHeight = "1.5";  // just the number
defaults.view = "alphabet";  // alphabet, phonic, phonic2, phonelist
defaults.language = 'bn'
defaults.blocklocation= '/rishida/scripts/block/bengali'
defaults.uifont = 'Noto Sans Bengali'; // font name per usage in CSS
defaults.uisize = "32";  // number of pixels (just the number)


