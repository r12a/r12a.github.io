var defaults = new Object;

defaults.font = '"SolaimanLipi", "Noto Sans Bengali WF"'; // font name per usage in CSS
defaults.size = "45";  // number of pixels (just the number)
defaults.rows = "2"; // number representing number of times 100px for height of box
defaults.lineHeight = "1.5";  // just the number
defaults.view = "alphabet";  // alphabet, phonic, phonic2, phonelist
defaults.language = 'bn'
defaults.blocklocation= '/scripts/block/bengali'
defaults.uifont = 'Noto Sans Bengali WF'; // font name per usage in CSS
defaults.uisize = "32";  // number of pixels (just the number)


