var defaults = new Object;

defaults.font = '"Aksara Bali WF"'; // font name per usage in CSS
defaults.size = "32";  // number of pixels (just the number)
defaults.rows = "2"; // number representing number of times 100px for height of box
defaults.view = "alphabet";  // alphabet, fontgrid
defaults.language = 'ban'
defaults.blocklocation= '/scripts/balinese/block'
defaults.uifont = 'Aksara Bali WF'; // font name per usage in CSS
defaults.uisize = "24";  // number of pixels (just the number)
defaults.ccbase = "";  // default base for combining characters


